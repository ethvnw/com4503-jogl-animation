/**
 * Name Node class for a scene graph.
 * @author Dr. Steve Maddock
 */
public class NameNode extends SGNode {

  /**
   * Create a new name node with the given name.
   * @param name The name of the node
   */
  public NameNode(String name) {
    super(name);
  }
}
